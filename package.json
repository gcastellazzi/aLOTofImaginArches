{
  "name": "alotofimaginarches",
  "version": "1.0.0",
  "description": "Interactive funicular and line-of-thrust analysis of masonry arches, in the browser",
  "license": "MIT",
  "author": "Giovanni Castellazzi",
  "homepage": "https://gcastellazzi.github.io/aLOTofImaginArches/app/",
  "repository": {
    "type": "git",
    "url": "https://github.com/gcastellazzi/aLOTofImaginArches.git"
  },
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "comment": [
    "There are no dependencies and there is no build step, on purpose.",
    "docs/app is plain ES modules, served as-is by GitHub Pages, so the",
    "source a student reads is the source that runs. This file exists only",
    "so that Node knows the .js files are modules and to name the test",
    "command; the browser never sees it."
  ]
}
